import { ArrowUpRight, Braces, Database, FileCheck2, Fingerprint, Link2, RotateCw } from "lucide-react";
import PageMeta from "../components/PageMeta"; import { ExternalLink, Status } from "../components/Ui"; import { links } from "../data/site";
const capabilities = [[Database,"Source reuse","Reuse resolved source work instead of starting from zero every time."],[FileCheck2,"Completeness handling","Make source state explicit so downstream workflows know what they received."],[RotateCw,"Long-source continuation","Continue across material that does not fit cleanly in a single response."],[Fingerprint,"Source identity","Keep provenance attached to the context passed into the AI workflow."]];
export default function LtaPage() { return <><PageMeta title="LTA" description="LTA is a managed source layer for public URLs the user already chose."/><header className="product-hero lta-hero wrap"><div><div className="product-title-row"><span className="product-icon lta large"><Link2 size={28} aria-hidden="true"/></span><Status live>Live</Status></div><p className="kicker">LTA by Eason Systems</p><h1>Your AI got the link. LTA brings the source.</h1><p className="page-lede">LTA turns supported public URLs into usable source context for AI workflows. It is built for links the user has already chosen.</p><div className="button-row"><a className="button blue" href={links.lta} target="_blank" rel="noreferrer">Open LTA <ArrowUpRight size={17} aria-hidden="true"/></a><a className="text-button" href={`${links.lta}docs`} target="_blank" rel="noreferrer">Read the API docs</a></div></div><div className="flow-card"><p className="flow-label">Source path</p><div className="flow-row"><span>Public URL</span><i>→</i><strong>LTA</strong><i>→</i><span>Source context</span></div><p className="source-list">YouTube · Instagram · X · GitHub · Web · PDF</p></div></header><section className="lta-definition wrap"><p className="section-index">What it does</p><h2>A source layer between a public link and the AI workflow using it.</h2><p>LTA handles retrieval details so an integration can work with the source instead of rebuilding support for each platform.</p></section><section className="capabilities-section wrap"><header><p className="section-index">Capabilities</p><h2>What LTA handles</h2></header><div className="capability-list">{capabilities.map(([Icon,title,text]) => <article key={title}><Icon size={20} aria-hidden="true"/><div><h3>{title}</h3><p>{text}</p></div></article>)}</div></section><section className="integration-band wrap"><div><Braces size={22} aria-hidden="true"/><p className="section-index">Integrations</p><h2>MCP for Claude and a REST API for product integrations.</h2></div><p>LTA can run inside an assistant workflow through MCP or behind another product through its REST API.</p><ExternalLink href={links.lta}>Visit LTA</ExternalLink></section></>; }
