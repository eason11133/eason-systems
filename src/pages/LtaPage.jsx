import { ArrowUpRight, Braces, Database, FileCheck2, Fingerprint, Link2, RotateCw } from "lucide-react";
import PageMeta from "../components/PageMeta"; import { ExternalLink, Eyebrow, Status } from "../components/Ui"; import { links } from "../data/site";
const capabilities = [[Database,"Source reuse","Reuse resolved source work instead of starting from zero every time."],[FileCheck2,"Completeness handling","Make source state explicit so downstream workflows know what they received."],[RotateCw,"Long-source continuation","Continue across material that does not fit cleanly in a single response."],[Fingerprint,"Source identity","Keep provenance attached to the context passed into the AI workflow."]];
export default function LtaPage() { return <><PageMeta title="LTA" description="LTA is a managed source layer for public URLs the user already chose."/><section className="product-hero lta-hero wrap"><div><div className="product-title-row"><span className="product-icon lta large"><Link2 size={28}/></span><Status live>Live</Status></div><Eyebrow>LTA by Eason Systems</Eyebrow><h1>Your AI got the link.<br/><span>LTA brings the source.</span></h1><p className="page-lede">A managed source layer for public URLs you already chose. LTA helps AI workflows obtain usable context without rebuilding source handling for every integration.</p><div className="button-row"><a className="button blue" href={links.lta} target="_blank" rel="noreferrer">Open LTA <ArrowUpRight size={17}/></a><a className="button ghost" href={`${links.lta}docs`} target="_blank" rel="noreferrer">View API</a></div></div><div className="flow-card"><p className="flow-label">A simpler source path</p><div className="flow-row"><span>Public URL</span><i>→</i><strong>LTA</strong><i>→</i><span>Usable context</span></div><div className="source-strip"><span>YouTube</span><span>Instagram</span><span>X</span><span>GitHub</span><span>Web</span><span>PDF</span></div></div></section><section className="statement section wrap"><Eyebrow>The role</Eyebrow><h2>LTA sits between a chosen public source and the AI workflow that needs to use it.</h2></section><section className="section wrap"><div className="section-heading"><div><Eyebrow>Capabilities</Eyebrow><h2>Source handling,<br/>as infrastructure.</h2></div><p>The details are designed to keep source context reliable after the first successful fetch.</p></div><div className="capability-grid">{capabilities.map(([Icon,title,text]) => <article key={title}><Icon size={22}/><h3>{title}</h3><p>{text}</p></article>)}</div></section><section className="integration-band section wrap"><div><Braces size={25}/><Eyebrow>Use it your way</Eyebrow><h2>MCP for Claude.<br/>REST for everything else.</h2></div><p>LTA can sit inside an AI assistant workflow through MCP or behind a product through its REST API.</p><ExternalLink href={links.lta}>Visit live product</ExternalLink></section></>; }
